import Constants from 'expo-constants';

export const API_BASE_URL = Constants.expoConfig?.extra?.API_BASE_URL ?? 'https://api-orange-sea.nonegroup.io.vn'; // Sửa port thành 8000 nếu server socket chạy ở port 8000
